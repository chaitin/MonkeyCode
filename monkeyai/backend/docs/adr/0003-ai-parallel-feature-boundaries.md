# 以业务能力作为 AI 并行开发边界

为了让多个 AI 在同一后端并行开发且尽量减少合并冲突，代码、API 契约片段、SQL 和测试都按业务能力归档，模型、知识库、技能、规则、MCP、专家等能力不再共用集中式资源包或 Admin、Agent 处理器目录。`go.mod`、应用组装、公共 HTTP 协议和构建配置保留为少量显式集成点，由独立集成任务串行修改；不使用隐式注册或全局公共类型来消除表面冲突。
