# 使用 Go 1.27 和标准库优先技术栈

后端最低语言版本确定为 Go 1.27，新代码优先使用能够减少样板并改善归属表达的 Go 1.27 特性；HTTP 处理器保持标准 `net/http` 接口，使用 chi 组织路由与中间件，配置、JSON、日志、并发、pprof 和测试直接使用标准库，不增加薄封装或重复标准库能力的小函数。pprof 使用独立的本机监听器，不挂载到业务端口。持久化使用 PostgreSQL、pgx 和 sqlc，迁移使用显式 SQL；只有标准库不存在合适能力时才引入第三方依赖，首期不引入 ORM、DI 容器、Redis、消息队列或独立分析数据库。
